/guia_apelia_mvp.docx
/proposta_mvp_apelia_v2.docx
/start.txt

# Variáveis de ambiente locais — NUNCA commitar (pode conter API keys, segredos)
.env
.env.local
.env.*.local

# Harness de e2e local (Playwright) — não faz parte do produto
/_e2e/
