# Pinado nas versões validadas em uso (imagem de build de 2026-08) — builds
# reproduzíveis. `create_agent` exige langchain 1.x; ranges abertos (>=) já
# quebraram a reprodutibilidade uma vez. Para atualizar: suba as versões aqui,
# rode a suíte (python -m pytest tests/) e valide um turno de chat real.
fastapi==0.139.0
uvicorn[standard]==0.49.0
langchain==1.3.11
langchain-core==1.4.8
langchain-ollama==1.1.0
langchain-anthropic==1.4.8
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1
pika==1.4.1
psycopg[binary]==3.3.4
langchain-openai==1.1.4
